spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_scrappi #Aqui falta normalizar la BD
    # definir DBA y control de user
    username:
    password:

#Por definir User para API
  security:
    user:
      name: admin
      password: #

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
  error:
    include-message: always
